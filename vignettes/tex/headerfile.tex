\documentclass[envcountsame,envcountchap]{svmono}
\usepackage{pslatex}	    % to use PostScript fonts
%% Set PDF 1.5 and compression, including object compression
%% Needed for MiKTeX -- most other distributions default to this
\ifx\pdfoutput\undefined
\else
  \ifx\pdfoutput\relax
  \else
    \ifnum\pdfoutput>0
      % PDF output
      \pdfminorversion=5
      \pdfcompresslevel=9
      \pdfobjcompresslevel=2
    \fi
  \fi
\fi

\usepackage{imakeidx}         % allows index generation with index in toc
\usepackage{graphicx}        % standard LaTeX graphics tool
                             % when including figure files
\usepackage{multicol}        % used for the two-column index
\usepackage[bottom]{footmisc}% places footnotes at page bottom
\usepackage{color}
\usepackage{hanging}
\usepackage{verbatim}
\usepackage[hyperfootnotes=false]{hyperref}
\usepackage{appendix}        % to have appendix page
\usepackage{bookmark}        % to modify the layout of toc

%\usepackage{Sweave}
%use this in Rnw files
%\SweaveOpts{keep.source=TRUE, eps=FALSE, prefix.string=figures/}
%\usepackage[bottom]{footmisc}% places footnotes at page bottom

% Math definitions are now in mathdefs.tex

\usepackage[round]{natbib} % to get references that are like in ecology papers
% \citet{} for inline citation name (year); \citep for citation in parens (name year)

\graphicspath{ {./figures/} }

\newcommand{\exbegin}[1]{%
\noindent
\begin{minipage}[t]{\linewidth}
\vskip 10pt plus 2pt minus 3pt 
\hrule
\vskip 10pt
\textbf{#1}
}
\newcommand{\exend}{%
\hrule
\vskip 10pt plus 2pt minus 3pt
\end{minipage}
}
\newcounter{example}[chapter]
\renewcommand{\theexample}{\thechapter.\arabic{example}}

\renewenvironment{example}[1]%
{\refstepcounter{example}\vspace{15pt}\par\noindent%
\textbf{Example \theexample\ (#1)}%
\par
\addtolength{\parskip}{\baselineskip}
\begin{hangparas}{.25in}{0}
\vspace{10pt}}%
{\vspace{10pt}\par
\addtolength{\parskip}{-\baselineskip}
\end{hangparas}
}

\renewenvironment{example}[1]%
{\refstepcounter{example}\vspace{15pt}\par\noindent%
\textbf{Example \theexample\ (#1)}%
\par
\addtolength{\parskip}{\baselineskip}
\addtolength{\parindent}{-\parindent}
\begin{itshape}
\vspace{10pt}}%
{\vspace{10pt}\par
\addtolength{\parskip}{-\baselineskip}
\addtolength{\parindent}{\parindent}
\end{itshape}
}

%Create a command for a footnote with no marker
\newcommand\blfootnote[1]{%
  \begingroup
  \renewcommand\thefootnote{}\footnote{#1}%
  \addtocounter{footnote}{-1}%
  \endgroup
}
